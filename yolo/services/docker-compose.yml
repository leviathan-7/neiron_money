services:
  yolo:
    command: test
    container_name: yolov8n
    image: yolov8n_image
    env_file:
      - .env
    build:
      context: ./yolov8n
    ports:
      - "8000:8000"
    volumes:
      - ${DATASET_PATH}:/app/dataset
      - ${SCRIPTS_PATH}:/app/scripts
      - ${OUTPUT_PATH}:/app/output
    deploy:
      resources:
        reservations:
          devices:
          - driver: nvidia
            capabilities:
              - gpu
              - utility # nvidia-smi
              - compute # CUDA